<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    tools:context="compioneerx1.github.whattopack.ui.ResultsActivity">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:id="@+id/imageView"
        android:src="@drawable/background"
        android:scaleType="centerCrop"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Forecast: Next 17 Days"
        android:textAlignment="center"
        android:id="@+id/inputSummaryTextView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:textColor="@color/colorTextIcons"
        android:textSize="30sp"
        android:background="@color/colorPrimary"
        android:alpha="0.9"
        />

    <android.support.v7.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/recyclerView"
        android:layout_below="@+id/saveTripButton"
        android:layout_marginTop="10dp"
        />


    <ListView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/forecastListView"
        android:layout_below="@+id/inputSummaryTextView"
        android:layout_marginTop="10dp"
        />


    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/saveTripButton"
        android:text="Save this Trip?"
        android:textColor="@color/colorTextIcons"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/inputSummaryTextView"
        android:background="@color/colorAccent"
        android:textSize="20sp"
        />

</RelativeLayout>
